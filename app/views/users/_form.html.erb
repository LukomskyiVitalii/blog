<%= form_with( model: user, local:true) do |form| %>
  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit "Edit" %>
    <%= link_to "Cancel", root_path %>
  </div>
<% end %>
 